{
  "name": "dawnpiece-launcher",
  "version": "0.1.0",
  "description": "Starter Electron launcher for DawnPiece profiles",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder"
  },
  "dependencies": {
    "@xmcl/user": "^4.3.0",
    "electron-updater": "^6.3.9"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "electron": "^34.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.3"
  }
}
